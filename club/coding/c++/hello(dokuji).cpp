#include <stdio.h>
#include <iostream>
#include <math.h>
#include <algorithm>
#include <queue>
#include <vector>
#include <stack>


int main(){

    using namespace std;

    vector<int> a;
    

    return 0;
}



/*
#include <iostream>

cin >> n;   nを入力

cout << n;  nを出力

cout << a << " ";

b.push_back{10};    最後に加える



#include <math.h>

ans = abs(-5)     =絶対値

ans = log10(100)    =10の何乗かを示す


#include <vector>

int a[10];
    これは同じ
vector<int> a[10];

vector(vector(char))
queue<int> q;
    無限に続く配列
q.push 3
q.pop   一番最初を消す

a.size()       
q.size()       queueのサイズを取得

queueは実行順番で書き込める
    途中で書き込んで後にpushすることができる

enqueue = pushだが、いれる順番を指定できる
dequeue = pop      

priority_queue
    数字を入れておくことでその順序に自動で並び替えて実行


stack
    上から順に行う
    追加したものは最初に行う。

    q.top 上から積み上げてきたものを順番に行う



while(!q.empty){

    int x = q.front();

    }

並び替え

sort[a.begin(),a.end()];

begin = 0番目   end = n+1番目 (一つ後ろ)

sort[a.rbegin(), a.rend()];

rbegin = n番目  rend = 0-1番目(一つ前) 
反対の意味になる

#include <algorithm> と<vector>

バブルソート

順序だててプログラムする    アルゴリズム

54321   

sort[a.begin(),a.end()];

[.]は～ののようなもの
q.push qに(10)を入力する
























*/