#include <stdio.h>
#include <hamako.h>



main()
{
  int left, right;

  left = getint("left: ");
  right = getint("right: ");

  printf("%d～%d までの和: %d\n", left, right,                        );

  return(0);
}

int betweenSum(int left, int right)
{

}
