#include <stdio.h>
#include <hamakou.h>

main()
{
	char c[], temp;
	int i, j, len;

	len = getstring("input: ", c);
	printf("入力文字列 = %s\n", c);

	i = ;
	j = ;
	while (i < j){
		;
		;
		;
		;
		;
	}
	printf("逆順文字列 = %s\n", c);

	return (0);
}
