#include <stdio.h>
#include <hamakou.h>



main()
{
  int a, b, c;

  a = getint("a: ");
  b = getint("b: ");

  c =          ;

  printf("a = %d, b = %d, max = %d", a, b, c);

  c =          ;

  printf("a = %d, b = %d, min = %d", a, b, c);

  return(0);
}

    max(            )
{
  int ans;

  if (x > y) {

  } else {

  }

  return(   );
}
