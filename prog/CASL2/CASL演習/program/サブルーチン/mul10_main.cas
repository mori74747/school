MAIN	START
;
; A番地の内容を値渡しで10倍する
;
	LD	GR1,A		; GR1にはA番地の値を入れて渡す（値渡し）
	CALL	MUL10V		; (GR1)を10倍するサブルーチン
	ST	GR1,A		; 10倍した値をA番地に格納
;
; B番地の内容をアドレス渡しで10倍する
;
	LAD	GR1,B		; GR1にはラベルBの番地を入れて渡す（アドレス渡し）
	CALL	MUL10A		; ((GR1))を10倍するサブルーチン
;
	RET
A	DC	10
B	DC	20
	END
